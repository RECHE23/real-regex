# Files under docs/*.md / docs/*.dox that are allowed to talk like a bench
# log. check_doc_voice_source.py still SCANS them (the counts are the work
# list) but does not fail on them. A path without a reason here is fail-closed.
#
# These two may talk like a bench log. That is a right, not a todo: the ledger's
# job is ns/B, and MEASUREMENT.md keeps the refutation archive on purpose.
# The Version cell is a stamp; the train journal lives in CHANGELOG.md.
# This file keeps the BENCHMARKS.md line because ns/B is the ledger's job.
# design.dox is deliberately absent.

docs/BENCHMARKS.md: the competitive ledger -- ns/B belong here; the train journal is CHANGELOG.md
docs/MEASUREMENT.md: the permit for every remaining number, plus the refutation archive

build/
*.o
*.profraw
*.profdata
*.so
__pycache__/
*.egg-info/
dist/
.DS_Store
.idea/
.vscode/

# libFuzzer writes generated inputs here; keep only hand-written seeds
build/fuzz/

# Doxygen output
build/doc/

# Stray compiler default output (scratch builds)
a.out
bindings/rust/fuzz/unicode_probe/target/

# CMAKE_EXPORT_COMPILE_COMMANDS writes build/compile_commands.json (already under build/); this
# covers a root-level symlink too, the common clangd convention (`ln -sf build/compile_commands.json .`)
compile_commands.json
.venv-gate/

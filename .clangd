# Portable baseline. clangd reads per-machine include paths from
# build/compile_commands.json (the build sets CMAKE_EXPORT_COMPILE_COMMANDS=ON).
# Keep machine-specific flags in your user config (~/.config/clangd/config.yaml).
CompileFlags:
  Add: [-std=c++20]
